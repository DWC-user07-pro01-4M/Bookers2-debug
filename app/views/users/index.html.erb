<h2>User info</h2>
<%= render 'info', user: current_user %>
<%= render 'users/index', book: @book %>

<h2 class="mt-3">New book</h2>
<%= render 'books/form', book: @book %>

<h2>Users</h2>
<%= render 'users/index',users: @users %>
